#
# Deprecated compatibility placeholder.
# Current authoritative secure flows are split into:
#   - docker-compose.mqtt-secure.yml
#   - docker-compose.coap-secure.yml
#   - docker-compose.ota-secure.yml
# This file intentionally contains no active overrides so it cannot drift away
# from the thesis workflow while still preserving the historical filename.
#
services: {}
